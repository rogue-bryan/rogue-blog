const routeType = Object.freeze({
    file: 'file',
    directory: 'directory',
})

const specialFolders = Object.freeze({
    home: 'home',
    blog: 'blog',
});

export {
    routeType,
    specialFolders,
};